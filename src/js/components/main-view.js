import React from 'react';

var MainView = React.createClass({
    displayName: 'MainView',

    render() {
        return (
            <div>

            </div>
        );
    }
});

export default MainView;
