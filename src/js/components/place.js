import React from 'react';

var PlaceView = React.createClass({
    displayName: 'PlaceView',

    render() {
        return (
            <div>
            </div>
        );
    }
});

export default PlaceView;
